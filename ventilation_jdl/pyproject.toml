[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ventilation_jdl"
version = "0.1.0"
description = "Überschlägige Jahresenergiemenge der Lüftung und Jahresdauerlinie auf Basis von DIN V 18599 und DWD-TRY-Wetterdaten"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.0",
    "numpy>=1.24",
    "matplotlib>=3.7",
]

[tool.setuptools.packages.find]
where = ["src"]
